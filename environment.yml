name: ragdoc
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    # Core MCP dependencies
    - fastmcp>=0.2.0
    - chromadb>=0.5.0
    - voyageai>=0.3.5
    - cohere>=5.0.0
    - python-dotenv>=1.0.0
    - chonkie[st]>=1.4.1
    - pyyaml>=6.0.0
    - rank-bm25>=0.2.2
    # CLI dependencies
    - rich
    - questionary
    - colorama
    # Dev/test
    - pytest>=7.0
    - pytest-asyncio>=0.21.0
